export const CONFIG = {
    // Grid
    cellSize: 20,
    gridCols: 60,
    gridRows: 50,

    // Timing
    tickMs: 100,

    // Snake
    initialLength: 2,
    growOnFood: 1,

    // Colors
    gridColor: "#071218",
    foodColor: "#ffeb3b",

    // Combat
    projectileSpeed: 2,      // Cells per tick
    armorColor: "#3b82f6",
    ammoColor: "#ef4444",
};
